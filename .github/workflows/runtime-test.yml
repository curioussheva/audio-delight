name: Runtime Test

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  runtime-test:
    runs-on: ubuntu-latest

    env:
      GRADLE_USER_HOME: ${{ github.workspace }}/.gradle
      NODE_ENV: development
      EXPO_NO_DEV_SERVER: 1

    steps:
      # 1. Checkout
      - uses: actions/checkout@v4

      # 2. Setup Node + PNPM (dengan cache)
      - uses: pnpm/action-setup@v4
        with:
          version: 9.0.0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      # 3. Install dependencies
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # 4. Setup Java + Android SDK
      - uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"
          cache: gradle

      - uses: android-actions/setup-android@v3

      # 5. Install NDK + CMake
      - name: Install NDK + CMake
        run: |
          yes | sdkmanager \
            "ndk;27.1.12297006" \
            "cmake;3.22.1"

      # 6. Install nasm
      - name: Install nasm
        run: |
          sudo apt-get update
          sudo apt-get install -y nasm

      # 7. Cache FFmpeg
      - name: Cache FFmpeg build
        id: cache-ffmpeg
        uses: actions/cache@v4
        with:
          path: android/app/src/main/cpp/libs/ffmpeg
          key: ffmpeg-android-${{ runner.os }}-${{ hashFiles('scripts/build-ffmpeg-android.sh') }}

      # 8. Build FFmpeg
      - name: Build FFmpeg for Android
        if: steps.cache-ffmpeg.outputs.cache-hit != 'true'
        run: bash scripts/build-ffmpeg-android.sh

      # 9. Configure Android paths
      - name: Configure Android paths
        run: |
          cat > android/local.properties << EOF
          sdk.dir=$ANDROID_HOME
          cmake.dir=$ANDROID_HOME/cmake/3.22.1
          EOF

      # 10. Inject Gradle init script
      - name: Inject Gradle init script
        run: |
          mkdir -p $GRADLE_USER_HOME/init.d
          cat > $GRADLE_USER_HOME/init.d/rn-plugins.gradle << EOF
          beforeSettings { settings ->
              settings.pluginManagement {
                  repositories {
                      google()
                      mavenCentral()
                      gradlePluginPortal()
                  }
                  includeBuild("${{ github.workspace }}/node_modules/@react-native/gradle-plugin")
                  includeBuild("${{ github.workspace }}/node_modules/expo-modules-autolinking/android/expo-gradle-plugin")
              }
          }
          EOF

      # 11. Patch RN Gradle Plugin
      - name: Patch RN Gradle Plugin
        run: |
          FILE=node_modules/@react-native/gradle-plugin/settings.gradle.kts
          if grep -q "foojay-resolver-convention" "$FILE"; then
            sed -i '/foojay-resolver-convention/d' "$FILE"
            echo "✅ Patched foojay"
          else
            echo "✅ Already patched"
          fi

      # 12. Setup Oboe
      - name: Setup Oboe
        run: |
          OBOE_DIR="android/app/src/main/cpp/oboe"
          mkdir -p "$OBOE_DIR"
          curl -L https://github.com/google/oboe/archive/refs/tags/1.9.0.tar.gz \
          | tar -xz --strip-components=1 -C "$OBOE_DIR"

      # 13. Clean CMake & native caches (untuk memastikan build bersih)
      - name: Clean CMake & native caches
        working-directory: android
        run: |
          chmod +x gradlew
          ./gradlew clean || true
          rm -rf app/.cxx app/build/.cxx
          echo "✅ Native caches cleaned"

      # 14. Generate Codegen
      - name: Generate Codegen
        working-directory: android
        run: |
          ./gradlew generateCodegenArtifactsFromSchema \
            --no-daemon \
            --rerun-tasks \
            -PnewArchEnabled=true \
            -PreactNativeNewArch=true \
            -PhermesEnabled=true

      # 15. Run generateAutolinkingNewArchitectureFiles
      - name: Run generateAutolinkingNewArchitectureFiles
        working-directory: android
        run: |
          ./gradlew generateAutolinkingNewArchitectureFiles \
            --no-daemon \
            --rerun-tasks \
            -PnewArchEnabled=true \
            -PreactNativeNewArch=true \
            -PhermesEnabled=true

      # 16. 🔥 EMBED EXPO BUNDLE (kunci utama)
      - name: Embed Expo bundle
        run: |
          echo "📦 Embedding Expo bundle..."
          npx expo export:embed --platform android
          
          # Verifikasi bundle
          if [ -f android/app/src/main/assets/index.android.bundle ]; then
            echo "✅ Bundle size: $(wc -c < android/app/src/main/assets/index.android.bundle) bytes"
          else
            echo "❌ Bundle not found! Aborting."
            exit 1
          fi

      # 17. Build Debug APK (dengan bundle embedded)
      - name: Build Debug APK
        working-directory: android
        run: |
          ./gradlew assembleDebug \
            --no-daemon \
            -PnewArchEnabled=true \
            -PreactNativeNewArch=true \
            -PhermesEnabled=true \
            -PreactNativeDevServerPort= \
            -PreactNativeDevServerEnabled=false

      # 18. Enable KVM Hardware Acceleration
      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm-rules.rules
          sudo udevadm control --reload-rules && sudo udevadm trigger --name-match=kvm

      # 19. Run on emulator & capture logs
      - name: Run on emulator & capture logs
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 30
          target: aosp_atd
          arch: x86_64
          profile: pixel_5
          cores: 2
          disable-animations: true
          emulator-options: -no-window -gpu swiftshader_indirect -no-snapshot -noaudio -no-boot-anim -no-metrics -camera-back none -camera-front none -memory 2048
          script: ./scripts/test-emulator.sh

      # 20. Upload Logcat Full
      - name: Upload Logcat Full
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: Logcat-Full
          path: logcat-full.log
          retention-days: 7

      # 21. Upload Audio Debug Log
      - name: Upload Audio Debug Log
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: Audio-Debug-Log
          path: audio-debug.log
          retention-days: 7

      # 22. Upload Build Log
      - name: Upload Build Log
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: Build-Log
          path: android/build.log
          retention-days: 7