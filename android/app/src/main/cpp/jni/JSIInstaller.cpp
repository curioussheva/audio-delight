#include "JSIInstaller.h"

namespace pristine {

void installJSI(
    JavaVM*,
    facebook::jsi::Runtime*
) {

    // future:
    // install host objects
    // install visualizer shared memory
    // install realtime DSP API
}

}