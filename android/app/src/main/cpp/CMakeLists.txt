cmake_minimum_required(VERSION 3.22.1)
project("pristineaudio_engine")

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

add_subdirectory(oboe)

add_library(
    pristineaudio_engine
    SHARED
    Native-lib.cpp
    AudioEngine.cpp
    NativePristineAudio.cpp
)


target_include_directories(
    pristineaudio_engine
    PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}
)

target_link_libraries(
    pristineaudio_engine
    oboe
    log
    android
    OpenSLES
    atomic
    m
)




# Codegen output path (generated by generateCodegenArtifactsFromSchema)                                         set(CODEGEN_OUTPUT_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../../../../build/generated/source/codegen")                                              add_library(                                                pristineaudio_engine                                    SHARED                                                  Native-lib.cpp                                          AudioEngine.cpp                                         NativePristineAudio.cpp                             )                                                                                                               # Oboe sebagai subdirectory                             add_subdirectory(oboe)                                                                                          target_include_directories(                                 pristineaudio_engine                                    PRIVATE                                                     ${CMAKE_CURRENT_SOURCE_DIR}                             ${CMAKE_CURRENT_SOURCE_DIR}/oboe/include                # Codegen headers untuk TurboModule spec                ${CODEGEN_OUTPUT_DIR}/jni                               ${CODEGEN_OUTPUT_DIR}/jni/react/renderer/components/pristineaudio                                       )                                                                                                               target_link_libraries(                                      pristineaudio_engine                                    oboe                                                    android                                                 log                                                 )